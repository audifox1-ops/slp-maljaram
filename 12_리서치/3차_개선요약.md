# SLP.Docs 3차 개선 요약

## 개선 완료 항목 (8건)

### 1. alert() 제거 (hwpxExportService)
- `alert()` 호출 2곳 제거 → 에러는 throw로 상위 호출자(토스트 시스템)에서 처리
- 브라우저 차단 알림 대신 일관된 토스트 알림 사용

### 2. cancelEdit schedule 초기화 버그 수정 (StudentManagement)
- `cancelEdit`에서 `schedule` 필드 누락 → `{ day: '', time: '', frequency: '1' }` 초기화 추가
- 수정 취소 시 이전 학생의 스케줄 데이터가 남는 버그 해결

### 3. 영문 UI → 한국어 (HeroSection, StudentSidebar)
- HeroSection: "AI-Powered SLP Automation" → "AI 기반 전문 치료 서류 자동화"
- HeroSection: "Analysis Status" → "분석 상태", "100% Completed" → "100% 완료"
- StudentSidebar: "Student Directory" → "학생 목록", "Profiles" → "명"
- StudentSidebar: "Clear All Data" → "모든 데이터 삭제"
- StudentSidebar: "New" → "신규"

### 4. useDocumentGenerator 중복 코드 제거
- mock 데이터 생성 구조 5곳 중복 → `createMockMonthly()` 헬퍼 함수로 통합
- 가상 날짜 생성 로직 3곳 중복 → `getVirtualDates()` 헬퍼 함수로 통합
- 코드 약 50줄 감소

### 5. 테스트 추가 (3개 파일, 36개 테스트)
- `templateService.test.ts`: CRUD, 검증, 경계값 (10개)
- `mockDataService.test.ts`: 세션 생성, 영역별 분기, 목표 포함 (14개)
- `dashboardService.test.ts`: 통계 계산, 포맷 함수 (12개)

### 6-8. (이전에 이미 완료된 사항)
- AI 응답 스키마 검증
- 에러바운더리 개선
- 파일 업로드 크기 검증

## 테스트 결과

| 검증 항목 | 결과 |
|-----------|------|
| TypeScript 타입 체크 | ✅ 통과 |
| 단위 테스트 | ✅ 96/96 통과 (1 skipped) |
| 빌드 | ✅ 성공 (8.85s) |

## 추가 발견된 개선 사항 (미구현)

### 보안 (즉시 조치 필요)
1. Firestore 규칙 `allow read, write: if true` → 인증 필요
2. Firebase API 키 하드코딩 제거
3. Gemini API 키 프록시 서버로 이동

### 성능
4. StudentSidebar에서 `countInvalidDates` 반복 계산 → 사전 계산
5. 검색 디바운스/deferredValue 미적용
6. `React.memo` 미적용 컴포넌트 (DocumentToolbar, ScheduleValidationPanel)

### UX
7. 모달 포커스 트랩 미구현
8. `window.confirm()` → 커스텀 확인 다이얼로그
9. 다운로드 버튼 로딩 상태 미표시
10. 키보드 단축키 미지원

### 코드 품질
11. `any` 타입 19곳 → 구체적 타입으로 교체
12. `useDocumentPersistence`가 훅이 아닌 서비스 → 이름 변경 필요
13. 미사용 React import 제거

### 설정
14. `package.json`에서 devDependencies 순서 정리
15. `vite` 의존성 중복 제거
16. `temp_gen_hwpx.js` 루트 → scripts/로 이동
